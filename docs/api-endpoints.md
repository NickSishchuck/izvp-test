# API Endpoints
`POST /test` - for uploading the entire test after changing it

`POST /test` - for sending the test.
`GET /test` - for attending the test.

`GET /statistics` - getting statistics per test

`POST /auth` - for sending the user name and test label 
